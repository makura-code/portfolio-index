# ポートフォリオ インデックスページ 設計指示書

## 0. このページの目的（最重要）

Web制作会社の採用担当が、**2分以内に「この人にコーディングを任せられるか」を判断するための資料**。
アート作品ではなく審査資料。したがって次の優先順位を厳守する。

1. 実績が一覧で即座に把握できる
2. このページ自体のコード品質・レスポンシブ対応が、そのままスキルの証明になっている
3. 装飾は最小限。読み手の時間を奪わない

**読者**: Web制作会社の発注担当者（1日に何十人分の応募を見ている、忙しい、コードが読める）
**このページの唯一の仕事**: 応募文に貼る1本のURLとして機能し、複数作品への導線をまとめること

---

## 1. 掲載する作品と順序

順序は「案件との近さ」で決める。制作順や思い入れの順ではない。

### セクションA: 静的サイト制作（最優先で見せる）

| 作品 | 何を証明するか |
|---|---|
| 革製品説明サイトシリーズ（kawagutsu-note / kawazaifu-note / kawa-note / oteire-note） | 同一の設計思想で複数サイトを量産できる。コンテンツサイトのマークアップ。 |
| zzz-agent-index | データ一覧・検索UIの実装。件数の多いコンテンツの構造化。 |

シリーズものは**1枚のカードにまとめて「4サイト」と明記**し、個別リンクはカード内に小さく並べる。
カードを4枚に分けると水増しに見えるので避ける。

### セクションB: Webアプリ

| 作品 | 何を証明するか |
|---|---|
| money-suite（家計簿・収入シミュレーター等を統合） | 複数アプリの統合という設計判断ができる。状態管理・localStorage。 |
| 着せ替えデモアプリ | インタラクティブUIの実装。 |

money-suiteは「localStorageの同一オリジン制約を解決するために4アプリを1サイトに統合した」という判断の経緯を1行書く。**問題を認識して構成で解決した**という事実は、初心者の作品集にはほぼ出てこないので効く。

### 掲載しないもの

- ローカルのみで未公開のもの（SUNSET PASS等）→ リンク先が動かないものは減点対象
- Godotのゲーム（retro-racer）→ 今回の応募先はWeb制作会社。畑違いの作品は焦点をぼかす
- 作りかけ・バグが残っているもの

---

## 2. ページ構成

```
┌────────────────────────────────┐
│ 名前 / 一行の肩書き              │  ← ヒーローは巨大な装飾ではなく名刺
│ 対応範囲を3〜4語で              │
├────────────────────────────────┤
│ 対応できること（箇条書き5行以内） │  ← 発注者が探している情報
├────────────────────────────────┤
│ 制作実績                        │
│  ┌──────────┐ ┌──────────┐    │
│  │ カード    │ │ カード    │    │
│  └──────────┘ └──────────┘    │
├────────────────────────────────┤
│ 使用技術 / 制作環境              │
├────────────────────────────────┤
│ 連絡先（クラウドワークスへの導線） │
└────────────────────────────────┘
```

### 作品カードの中身（テンプレート）

各カードに必ず入れる4要素:

1. **サイト名**
2. **1〜2行の説明**（何のサイトか。「〜を作りました」ではなく何であるかを書く）
3. **使用技術**（HTML / CSS / JavaScript など、モノスペースのラベルで）
4. **実装のポイント（1行）** ← ここが差別化の核

「実装のポイント」の例:
- 「4サイト共通のCSS設計を切り出し、テーマカラーの変更のみで展開できる構成にした」
- 「localStorageの同一オリジン制約を回避するため、4アプリを単一サイトのサブディレクトリに統合」
- 「件数の多い一覧を、JSでのフィルタリングとカード型レイアウトで一覧性を確保」

発注者が見たいのは見た目ではなく**判断の跡**。ここを書けると初心者の作品集から一段抜ける。

スクリーンショットは載せてよいが、**必須ではない**。用意できない場合は無理に画像を作らず、テキストとリンクで成立させる（画質の低い画像を貼るほうがマイナス）。

---

## 3. デザイン方針

### カラートークン（白×グリーン）

```css
--ink:    #14231C;  /* 本文。純黒は使わない */
--ground: #FFFFFF;  /* 地色 */
--paper:  #F4F7F4;  /* カード背景。わずかに緑を含む白 */
--green:  #1F7A4C;  /* 主色。見出し・リンク */
--accent: #2FBF71;  /* 差し色。ホバーとアクティブのみ。面で使わない */
--rule:   #DCE5DE;  /* 罫線 */
```

グリーンを面で大きく使わない。**白地の余白が主役で、緑は構造を示す線と文字**に使う。ベタ塗りの緑ヘッダーにすると量産テンプレート感が出る。

### タイポグラフィ

- 見出し: `Zen Kaku Gothic New` の Bold（字面が締まっていて日本語の見出しが決まる）
- 本文: `Noto Sans JP` の Regular
- 技術ラベル・数値: `JetBrains Mono`

モノスペースを技術ラベルに使うのは装飾ではなく**それがコードの仕事だという情報**を持たせるため。それ以外の箇所ではモノスペースを使わない。

### 使わないもの

- グラデーション背景
- 大きなヒーロー画像
- スクロール連動の派手なアニメーション
- 「Passionate Developer」的な英語のキャッチコピー

動きはホバー時の下線・軽い色変化のみ。`prefers-reduced-motion` に対応する。

---

## 4. 実装要件（ここが審査対象）

このページのコードそのものが提出物になるという前提で書く。

- **単一のHTMLファイル + 単一のCSSファイル**（ビルドツール不要。誰でも読める構成）
- セマンティックHTML: `header` / `main` / `section` / `article` / `footer` を正しく使う。`div`の入れ子で済ませない
- 見出しレベル（h1〜h3）を階層通りに使う。h1はページに1つ
- CSSはカスタムプロパティで色・余白を定義。マジックナンバーを散らさない
- クラス名の命名規則を統一する（BEM推奨。応募先がBEM記法を要求する案件もあるため、実践しておくと話せる材料になる）
- レスポンシブ: 375px / 768px / 1280px で崩れないこと。カードは1列→2列
- 画像には必ず `alt`。装飾画像は `alt=""`
- キーボードのTab移動でフォーカスリングが見えること
- `<title>` と `<meta name="description">` を適切に設定
- ファビコンを設定（未設定のデフォルトアイコンは雑さの証拠として見られる）
- 外部リンクは `target="_blank"` + `rel="noopener"`
- ブラウザのコンソールにエラー・警告を1つも出さない

---

## 5. 公開前チェックリスト

作り終わったら、この順で確認する。

- [ ] **掲載した全リンクを実際にクリックして開く**（Netlifyのビルド上限で落ちているサイトがないか。7月に上限到達しているので特に注意）
- [ ] スマホ実機で表示確認（開発者ツールのシミュレータだけでは不十分）
- [ ] コンソールエラーがゼロ
- [ ] 誤字脱字（発注者は文章も見ている）
- [ ] GitHubリポジトリを公開リンクとして載せる場合、リポジトリがpublicになっているか
- [ ] 個人情報（本名・住所・メールアドレス）が意図せず載っていないか

---

## 6. スコープ外（今回はやらない）

締切が近いので広げない。以下は後回し。

- ブログ機能・記事一覧
- お問い合わせフォーム（サーバーが必要になる。クラウドワークス経由の連絡で足りる）
- ダークモード
- 多言語対応
- 凝ったスクリーンショットの作り込み

**完成の定義**: 全リンクが動き、スマホで崩れず、コンソールが静かで、公開URLが1本ある状態。それ以上は今はいらない。
